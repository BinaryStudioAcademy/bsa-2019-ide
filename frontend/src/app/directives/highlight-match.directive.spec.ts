import { HighlightMatchDirective } from './highlight-match.directive';

describe('HighlightMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMatchDirective();
    expect(directive).toBeTruthy();
  });
});
