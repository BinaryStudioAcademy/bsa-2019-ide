import { HistorySearchPipe } from './history-search.pipe';

describe('HistorySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new HistorySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
