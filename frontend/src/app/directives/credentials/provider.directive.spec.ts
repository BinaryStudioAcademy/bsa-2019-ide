import { ProviderDirective } from './provider.directive';

describe('ProviderDirective', () => {
  it('should create an instance', () => {
    const directive = new ProviderDirective();
    expect(directive).toBeTruthy();
  });
});
