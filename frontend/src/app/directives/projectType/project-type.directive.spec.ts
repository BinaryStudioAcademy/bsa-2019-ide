import { ProjectTypeDirective } from './project-type.directive';

describe('ProjectTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectTypeDirective();
    expect(directive).toBeTruthy();
  });
});
