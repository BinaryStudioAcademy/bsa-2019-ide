import { UserAccessDirective } from './user-access.directive';

describe('UserAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new UserAccessDirective();
    expect(directive).toBeTruthy();
  });
});
