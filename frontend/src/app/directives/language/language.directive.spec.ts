import { LanguageDirective } from './language.directive';

describe('LanguageDirective', () => {
  it('should create an instance', () => {
    const directive = new LanguageDirective();
    expect(directive).toBeTruthy();
  });
});
