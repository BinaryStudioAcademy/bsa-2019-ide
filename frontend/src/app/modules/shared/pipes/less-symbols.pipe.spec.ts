import { LessSymbolsPipe } from './less-symbols.pipe';

describe('LessSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new LessSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
