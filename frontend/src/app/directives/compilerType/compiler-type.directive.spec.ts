import { CompilerTypeDirective } from './compiler-type.directive';

describe('CompilerTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new CompilerTypeDirective();
    expect(directive).toBeTruthy();
  });
});
